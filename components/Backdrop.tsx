import { SignalField } from "./SignalField";

/**
 * Landing-page ambient backdrop — the full-strength SignalField: a calm,
 * animated "attention field" beneath the page. Fixed behind all content.
 */
export function Backdrop() {
  return <SignalField />;
}
